[flake8]
max-line-length = 100
# Black compatibility: E203 (slice whitespace) and W503 (line break before
# binary operator) conflict with black's formatting.
extend-ignore = E203, W503
exclude =
    .git,
    __pycache__,
    build,
    dist,
    .venv,
    venv
